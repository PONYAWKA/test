patterns:
    $greet = (привет|здравствуй|здравствуйте|добрый день|добрый вечер|доброе утро|хай)
    $help = (помощь|что ты умеешь|что вы умеете|меню|как записаться на то|как оформить то)
    $thanks = (спасибо|благодарю|благодарим|спс)
    $bye = (пока|до свидания|до встречи|всего доброго)
    $hours = (режим работы|часы работы|график работы|когда работаете|когда открыты|до скольки)
    $address = (адрес|где находитесь|где вы|как доехать|как добраться)
    $services = (услуги|что делаете|что обслуживаете|ремонт|шиномонтаж|диагностика)

    $entryService = (записаться на то|запиши меня на то|оформить то|как пройти то|как пройти то-1|тех обслуживание пройти|пройти тех обслуживание|пройти то|то надо|подошло время то)

    $faq_cost = (сколько стоит|цена|дорог|деньг*|плати*)*(то|тех обслуж|то-1|тео|техобслуживание)*
    $faq_need_schedule = (нужно ли|когда делать|ехать|график|план)*(то|тех обслуж|тео)*
    $faq_complaint = (почините*|повредил*|повредили*|требую починить|роспотребнадзор|отказал? менять фильтр|странный запах)*
    $faq_why = зачем* *(то|тех обслуж)*
    $faq_noise = (*купил*|*машин*|*гудит*|*стала*|*гудеть*|*30*|*шум*|*30*)
    $faq_discounts = (*скид*|*акци*)*(то|тех обслуж | тех)*
    $faq_duration = (сколько*по времени|занимает|долго ли)*(то|тех обслуж)*
    $faq_scope = (*что входит*|*состав работ*|*план работ*|работы| *планово* )*(то|тех обслуживание)*

    $sm_howareyou = (как дела|как поживаешь|как вы поживаете)
    $sm_positive = (хорошо|супер|здорово|отлично|нормально)
    $sm_negative = (плохо|хреново|неоч|не очень|так себе)
    $sm_name = (как тебя зовут|ты кто|кто ты)
    $sm_introduce = (а я|меня зовут|я)
