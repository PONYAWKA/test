patterns:
    $greet = (* привет *|* здравствуй *|* здравствуйте *|* добрый день *|* добрый вечер *|* доброе утро *|* хай *)
    $help = (* помощь *|* что ты умеешь *|* что вы умеете *|* меню *|* как записаться на то *|* как оформить то *)
    $thanks = (* спасибо *|* благодарю *|* благодарим *|* спс *)
    $bye = (* пока *|* до свидания *|* до встречи *|* всего доброго *)
    $hours = (* режим работы *|* часы работы *|* график работы *|* когда работаете *|* когда открыты *|* до скольки *)
    $address = (* адрес *|* где находитесь *|* где вы *|* как доехать *|* как добраться *)
    $services = (* услуги *|* что делаете *|* что обслуживаете *|* ремонт *|* шиномонтаж *|* диагностика *)

    $entryService = (
        * записаться * то *|
        * запиши * то *|
        * оформить * то *|
        * пройти * то *|
        * сделать * то *|
        * тех обслуживание * пройти *|
        * подошло время * то *|
        * то надо *
    )

    $faq_cost = (
        * сколько * стоит * (то|тех обслуживание|то-1) *|
        * цена * (то|тех обслуживание) *|
        * дорого * (то|тех обслуживание|тео) *|
        * дорогое * (то|тех обслуживание|тео) *|
        * дорогое выйдет * (то|тео) *
    )

    $faq_need_schedule = (
        * нужно ли * (то|тех обслуживание|тео) *|
        * когда * (делать|ехать) * (то|тех обслуживание) *|
        * график * (то|тех обслуживания) *|
        * план * (то|тех обслуживания) *
    )

    $faq_complaint = (
        * почините * то * сломали *|
        * повредили * бампер *|
        * требую починить *|
        * роспотребнадзор *|
        * отказались * менять * фильтры *|
        * странный запах * машине *
    )

    $faq_why = (* зачем * (то|тех обслуживание) *)

    $faq_noise = (* купил * машину * гудит *|* стала * гудеть *|* гудит * 3000 *|* шум * 3000 *)

    $faq_discounts = (* скидк * (то|тех обслуживание) *|* акции * (то|тех обслуживание) *)

    $faq_duration = (
        * сколько * по времени * (то|тех обслуживание) *|
        * сколько * занимает * (то|тех обслуживание) *|
        * долго ли * (то|тех обслуживание) *
    )

    $faq_scope = (* что входит * (то|тех обслуживание) *|* состав работ * (то|тех обслуживание) *|* план работ * (то|тех обслуживание) *)

    $sm_howareyou = (* как дела *|* как поживаешь *|* как вы поживаете *)
    $sm_positive = (* хорошо *|* супер *|* здорово *|* отлично *|* нормально *)
    $sm_negative = (* плохо *|* хреново *|* неоч *|* не очень *|* так себе *)
    $sm_name = (* как тебя зовут *|* ты кто *|* кто ты *)
    $sm_introduce = (* а я *|* меня зовут *|* я *)
